import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Off Plans Projects | Joya Properties",
};

export default function OffPlansProjectsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
